# Models Directory

This folder is intended for storing trained machine learning models such as:

- .pkl (pickle files)
- .joblib files

In this project, models such as Logistic Regression, Random Forest, XGBoost, and LightGBM are trained.

Due to file size constraints, trained models are not included in this repository.

You can generate and save models locally using the training pipeline.
